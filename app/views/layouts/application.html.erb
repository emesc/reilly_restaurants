<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Ruby on Rails and React" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/assets/javascripts/modenizr-custom" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="top-bar">
      <div class="top-bar-left">
        <ul class="dropdown menu" data-dropdown-menu>
          <h4>
            <%= link_to "Reilly Restaurants", root_path, class: "menu-text" %>
          </h4>
        </ul>
      </div>
      <div class="top-bar-right">
        <ul class="menu">
          <%= link_to "manage restaurants", "/admin", class: "menu-text" %>
        </ul>
      </div>
    </div>
    <div class="row">
      <div class="large-12 columns">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
